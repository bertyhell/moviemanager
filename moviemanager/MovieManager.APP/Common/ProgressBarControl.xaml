<UserControl x:Class="MovieManager.APP.Common.ProgressBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300" DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <ProgressBar Height="auto" HorizontalAlignment="Stretch" Name="progressbar" VerticalAlignment="Stretch" Width="auto" IsIndeterminate="{Binding IsIndeterminate}" />
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding ProgressString}"></TextBlock>
    </Grid>
</UserControl>
